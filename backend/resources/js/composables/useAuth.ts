/**
 * Auth composable placeholder.
 *
 * Separation rationale:
 * - services: data access + protocol details
 * - composables: UI state orchestration/use-case composition
 */
export const useAuth = () => {
  return {};
};

